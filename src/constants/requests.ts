export const BaseURL = 'https://pokeapi.co/api/v2';

export const RequestURL = {
    GetPokemonBriefs: `${BaseURL}/pokemon`,
    GetTypeBriefs: `${BaseURL}/type`,
};

export const DefaultPokemonLimit = 2000;
